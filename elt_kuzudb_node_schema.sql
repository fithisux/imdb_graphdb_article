CREATE NODE TABLE REGION_CODE(name STRING, alpha2 STRING, PRIMARY KEY (alpha2));
CREATE NODE TABLE LANGUAGE_CODE(alpha2 STRING, English STRING, PRIMARY KEY (alpha2));
CREATE NODE TABLE GENRE(genre STRING, PRIMARY KEY (genre));
CREATE NODE TABLE DISTRIBUTION_TYPE(distribution_type STRING, PRIMARY KEY (distribution_type));
CREATE NODE TABLE JOB_CATEGORY(category STRING, PRIMARY KEY (category));
CREATE NODE TABLE TITLE_TYPE(title_type STRING, PRIMARY KEY (title_type));
CREATE NODE TABLE ATTRIBUTES(attribute STRING, PRIMARY KEY (attribute));
CREATE NODE TABLE CHARACTERS(somecharacter STRING, PRIMARY KEY (somecharacter));
CREATE NODE TABLE PRIMARYPROFESSION(profession STRING, PRIMARY KEY (profession));
CREATE NODE TABLE NAME_BASICS(nconst STRING, primaryName STRING, birthYear INT32, deathYear INT32, PRIMARY KEY (nconst));
CREATE NODE TABLE TITLE_AKAS(ID STRING, ordering INT32, title STRING, isOriginalTitle BOOLEAN, PRIMARY KEY (ID));
CREATE NODE TABLE TITLE_BASICS(tconst STRING, primaryTitle STRING, originalTitle STRING, isAdult boolean, startYear INT32, endYear INT32 ,runtimeMinutes INT32, PRIMARY KEY (tconst));
CREATE NODE TABLE TITLE_EPISODE(epitconst STRING, seasonNumber INT32, episodeNumber INT32, PRIMARY KEY (epitconst));
CREATE NODE TABLE TITLE_PRINCIPALS(ID STRING, ordering INT32, job STRING, PRIMARY KEY (ID));
CREATE NODE TABLE TITLE_RATINGS(tconst STRING, averageRating DOUBLE, numVotes INT32, PRIMARY KEY (tconst));